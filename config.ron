Config(
    storage: StorageConfig(
        db_config: DbConfig(
            path: "./data",
            max_size: 1099511627776, // 1TB.
        ),
    ),
    central: CentralSourceConfig(
        url: "https://alpha4.starknet.io/",
        retry_base_millis: 100,
        retry_max_delay_millis: 10000,
        max_retries: 5,
    ),
)